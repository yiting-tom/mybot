## 1. Trim message data model (Trimmed message data model)

- [x] 1.1 In `mybot/bus/events.py`, remove `media: list[str]` and `metadata: dict[str, Any]` from `InboundMessage`, and remove `media: list[str]`, `reply_to: str | None`, and `metadata: dict[str, Any]` from `OutboundMessage`. Drop the now-unused `Any` import if nothing else uses it.
- [x] 1.2 In the same file, change `channel: str` to `channel: Literal["cli", "system"]` on both `InboundMessage` and `OutboundMessage`, and add `from typing import Literal`. This realizes the channel-typing portion of the spec requirement "Trimmed message data model".
- [x] 1.3 In `mybot/bus/events.py`, add two explicit typed fields on `OutboundMessage` to replace the dropped `metadata` dict: `is_progress: bool = False` and `is_tool_hint: bool = False`. These preserve the load-bearing progress flag that was previously stored under `metadata["_progress"]` / `metadata["_tool_hint"]` (per the "Progress messages are flagged via typed fields" scenario in the spec requirement "Trimmed message data model").

## 2. Update callers (Two valid publisher patterns, Outbound queue has a single REPL consumer)

- [x] 2.1 In `mybot/agent/loop.py`, audit every `InboundMessage(...)` and `OutboundMessage(...)` construction and read site. Specifically: (a) replace each `msg.metadata.get("message_id")` read (around lines 342 and 412) with the literal `None` it always returned in practice; (b) replace `msg.media if msg.media else None` (around line 421) with `None`; (c) remove `metadata=msg.metadata or {}` kwargs from `OutboundMessage(...)` constructions (around lines 302, 430, 450); (d) replace the `_bus_progress` callback's `metadata={"_progress": True, "_tool_hint": tool_hint}` shape with `is_progress=True, is_tool_hint=tool_hint` direct kwargs. Confirm each remaining construction still uses one of the two valid `channel` values from the spec requirement "Two valid publisher patterns".
- [x] 2.2 In `mybot/agent/subagent.py` around line 200, audit the `InboundMessage(channel="system", ...)` construction used for subagent fan-in and remove any kwargs that target the deleted fields. Confirm the `channel="system"` value still aligns with the spec requirement "Two valid publisher patterns".
- [x] 2.3 In `mybot/agent/tools/message.py`, audit the `OutboundMessage(...)` construction inside the `MessageTool` send path. Remove the `media=media or []` kwarg and the `metadata={"message_id": message_id}` kwarg — neither field exists any longer and neither is read by any consumer. Update the `execute()` signature to drop the now-unused `media` and `message_id` parameters if they are no longer referenced elsewhere; otherwise leave the parameter signature alone but stop passing the values into `OutboundMessage`. Confirm this caller still publishes through `bus.publish_outbound` so the spec requirement "Outbound queue has a single REPL consumer" continues to hold.
- [x] 2.4 In `mybot/cli/commands.py`, audit every `InboundMessage(...)` and `OutboundMessage(...)` construction across the four CLI command paths that instantiate a `MessageBus()`. Remove any kwargs that target the deleted fields. Update the REPL outbound consumer at the line that currently reads `if msg.metadata.get("_progress"):` to instead read `if msg.is_progress:`, satisfying the "Progress messages are flagged via typed fields" scenario from the spec requirement "Trimmed message data model".
- [x] 2.5 Run `python -c "import mybot.cli.commands; import mybot.agent.loop; import mybot.agent.subagent; import mybot.agent.tools.message; print('OK')"` to confirm every caller still imports cleanly with the trimmed dataclass signatures.

## 3. Document scope (Documented contract on MessageBus, In-process scope of MessageBus)

- [x] 3.1 In `mybot/bus/queue.py`, replace the existing `MessageBus` class docstring with one that states: (a) the bus is in-process only and per-CLI-invocation as required by the spec requirement "In-process scope of MessageBus", (b) the bus never crosses bot boundaries, (c) the two valid publisher patterns from the spec requirement "Two valid publisher patterns", (d) the sole consumer of `consume_inbound` is `AgentLoop.run()`, and (e) `agent.process_direct(...)` bypasses the bus while the REPL renderer is the sole `consume_outbound` consumer.
- [x] 3.2 In `mybot/bus/queue.py`, replace the legacy module docstring "Async message queue for decoupled channel-agent communication." with text that aligns with the new scope: REPL ↔ agent queue plus subagent fan-in, in-process only.
- [x] 3.3 In `mybot/bus/events.py`, replace the legacy module docstring "Event types for the message bus." with text noting that the two message classes serve only the in-process REPL ↔ agent path and the subagent fan-in path. Update the per-class docstrings on `InboundMessage` and `OutboundMessage` so they no longer say "received from a chat channel" / "to send to a chat channel" — describe them in terms of the actual two channels (`cli`, `system`).
- [x] 3.4 In `mybot/bus/__init__.py`, replace the module docstring "Message bus module for decoupled channel-agent communication." with text aligned to the new scope.
- [x] 3.5 In `README.md`, change the "Project layout" line `bus/  🚌 In-process message routing` to `bus/  🚌 REPL ↔ agent queue + subagent fan-in`, satisfying the README portion of the spec requirement "Documented contract on MessageBus".

## 4. Verification

- [x] 4.1 Run `python -m pytest tests/ -q` and confirm the existing 17 tests still pass after the data-model trim.
- [ ] 4.2 Manually smoke-test the REPL: run `mybot test "say hello"` (single-shot) and confirm the response renders without error. This exercises the spec requirement "Outbound queue has a single REPL consumer" via the bus path.
- [ ] 4.3 Manually smoke-test interactive REPL: run `mybot test`, type a short message, confirm the response renders, type `exit`. This exercises both `publish_inbound` (REPL stdin) and `consume_outbound` (REPL renderer) paths from the spec requirement "Two valid publisher patterns".
- [ ] 4.4 Manually smoke-test the cron bypass path: run `mybot test daemon --once` with one due cron job and confirm the job runs and `process_direct` returns its response without touching the bus, per the "cron and heartbeat bypass the bus" scenario in the spec requirement "Two valid publisher patterns".
- [x] 4.5 Run `spectra validate trim-bus-data-model` and confirm green.
